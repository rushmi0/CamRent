package org.camrent.database.gen

import org.camrent.database.table.CustomersTable
import org.jetbrains.exposed.sql.max
import org.jetbrains.exposed.sql.selectAll

object GeneratePrimaryKey {

//    fun genPrimaryKey(tableName: String): String {
//        val maxID = CustomersTable.slice(CustomersTable.customerID.max()).selectAll()
//            .single()[CustomersTable.customerID.max()].toString()
//
//        return
//    }

}